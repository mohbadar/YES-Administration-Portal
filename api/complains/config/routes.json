{
  "routes": [
    {
      "method": "GET",
      "path": "/complains",
      "handler": "complains.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/complains/count",
      "handler": "complains.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/complains/:id",
      "handler": "complains.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/complains",
      "handler": "complains.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/complains/:id",
      "handler": "complains.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/complains/:id",
      "handler": "complains.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
